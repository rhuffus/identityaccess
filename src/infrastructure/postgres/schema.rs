table! {
    users (id) {
        id -> Integer,
        uuid -> Uuid,
        first_name -> Varchar,
        last_name -> Varchar,
        username -> Varchar,
        password -> Varchar,
        email -> Varchar,
    }
}
